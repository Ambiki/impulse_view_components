<%= trigger %>

<%= render(Impulse::BaseRenderer.new(**@system_args)) do %>
  <dialog id="<%= dialog_id %>" class="awc-dialog" aria-labelledby="<%= trigger_id %>" data-action="close->awc-dialog#handleClose" data-target="awc-dialog.dialog">
    <%= header %>
  </dialog>
<% end %>
